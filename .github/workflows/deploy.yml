name: Deploy
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "Hello Claw Club"
      - run: mkdir -p output && echo "OK" > output/status.txt
      - uses: actions/upload-artifact@v3
        with:
          name: output
          path: output/